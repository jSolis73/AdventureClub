import React from 'react'
import classes from './CurrentPlace.module.css'

const CurrentPlace = props => {
	return (
		<div className={classes.CurrentPlace}>

		</div>
	)
}

export default CurrentPlace